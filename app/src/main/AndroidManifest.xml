<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.tcc1_abdiel">

    <application
        android:allowBackup="true"
        android:icon="@drawable/ifsc"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ifsc"
        android:supportsRtl="true"
        android:theme="@style/Theme.TCC1Abdiel"
        tools:targetApi="31">

        <!-- SignUpActivity -->
        <activity
            android:name="com.example.tcc1_abdiel.SignUpActivity"
            android:label="Sign Up"
            android:exported="true">
            <!-- Intent filter for the main action -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- MainActivity -->
        <activity
            android:name="com.example.tcc1_abdiel.MainActivity"
            android:label="Main Activity"
            android:exported="true"
             />

        <activity
            android:name="com.example.tcc1_abdiel.SignInActivity"
            android:label="Sign In"
            android:exported="true"></activity>

        <!-- Other activities and configurations -->

    </application>

</manifest>
